# VS Code Agents Instructions

This file provides instructions for AI coding agents working with the VS Code codebase.

For detailed project overview, architecture, coding guidelines, and validation steps, see the [Copilot Instructions](.github/copilot-instructions.md).

---

## IC-dev Project Note

This repository is also the base for **IC-dev**, a customized IDE derived from VS Code.
When working on IC-dev tasks, also read `.goosehints` for project context and `icdev/state.md` for current progress.
IC-dev customizations live on the `icdev-dev` branch; the `main` branch tracks upstream microsoft/vscode cleanly.
